import java.util.*;

public class Util {
    public static boolean canColorEntireRow(int[] sequence, int M){
        // 假设 sequence 是给定的黑色块序列，M 是列数
        int length = sequence.length;
        if(length == 0){
            return true;
        }
        if(length >= 1){

        }
    }
}
